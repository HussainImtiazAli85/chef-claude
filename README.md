# Chef Claude (React)

AI recipe assistant (work in progress).

React app scaffolded with Vite for fast development and simple builds.

Repository: https://github.com/HussainImtiazAli85/chef-claude

## Requirements

- Node.js (tested with Node v24+)
- npm (comes with Node)

## Getting started

Install dependencies:

```bash
npm install
```

Start the dev server:

```bash
npm run dev
```

Build for production:

```bash
npm run build
```

Preview the production build locally:

```bash
npm run preview
```

## Code quality

Run the linter:

```bash
npm run lint
```

## Git workflow (for reviews)

Typical commit/push cycle:

```bash
git status
git add -A
git commit -m "Your message"
git push
```

## Tech stack

- React
- Vite
- ESLint
